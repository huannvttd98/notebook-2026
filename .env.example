# Cổng chạy app (Nginx sẽ proxy về cổng này)
PORT=3100

# Thành phố hiển thị thời tiết (vd: Ho Chi Minh, Ha Noi, Da Nang)
WEATHER_CITY=Ha Noi

# Đặt 'production' khi chạy trên server
NODE_ENV=production
